#!/bin/sh

##
# {{ ansible_managed }}
#

set -e
errors=$(/usr/sbin/php7.3-fpm --fpm-config /home/{{ sansible_php_fpm_user }}/etc/php-fpm.conf -t 2>&1 | grep "\[ERROR\]" || true);
if [ -n "$errors" ]; then
	echo "Please fix your configuration file..."
	echo $errors
	exit 1
fi
exit 0
