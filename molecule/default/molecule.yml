---

driver:
  name: docker

platforms:
  - name: sansible_php-trusty
    image: ubuntu:trusty
  - name: sansible_php-xenial
    image: ubuntu:xenial
  - name: sansible_php-bionic
    image: ubuntu:bionic

provisioner:
  name: ansible
  config_options:
    defaults:
      hash_behaviour: merge
  lint:
    name: ansible-lint

lint:
  name: yamllint
  options:
    config-data:
      extends: default
      ignore: .venv_ansible*
      rules:
        line-length:
          max: 159
        truthy: disable

dependency:
  name: galaxy
  options:
    role-file: requirements.yml

scenario:
  name: default

verifier:
  name: testinfra
  lint:
    name: flake8
